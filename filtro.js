var piramide = "a";
var alturap=4;
var cadena;

listafilas = new Array(alturap);

for (let i = 0; i < listafilas.length; i++) {
  cadena=piramide.charAt(i);
  listafilas[i]=cadena;
}
do {
    repe++
} while (repe != alturap);